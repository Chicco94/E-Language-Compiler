The Language E
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of E==

===Literals===
Integer literals //Integer// are nonempty sequences of digits.


Double-precision float literals //Double// have the structure
indicated by the regular expression ``digit+ '.' digit+ ('e' ('-')? digit+)?`` i.e.\
two sequences of digits separated by a decimal point, optionally
followed by an unsigned or negative exponent.


Character literals //Char// have the form
``'``//c//``'``, where //c// is any single character.


String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.




PIdent literals are recognized by the regular expression
`````letter (letter | digit | '_' | ''')*`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in E are the following:
  | ``bool`` | ``break`` | ``char`` | ``continue``
  | ``def`` | ``double`` | ``else`` | ``false``
  | ``for`` | ``if`` | ``in`` | ``int``
  | ``match`` | ``return`` | ``string`` | ``switch``
  | ``true`` | ``var`` | ``void`` | ``while``

The symbols used in E are the following:
  | ( | ) | { | }
  | , | : | [ | ]
  | := | match _ | & | *
  | ! | - | ** | /
  | // | % | %% | +
  | .. | ..! | < | >
  | <= | >= | == | !=
  | && | || | |= | &=
  | += | -= | *= | //=
  | /= | %= | %%= | **=
  | ; | :] |  | 

===Comments===
Single-line comments begin with //.Multiple-line comments are  enclosed with /* and */.

==The syntactic structure of E==
Non-terminals are enclosed between < and >. 
The symbols -> (production),  **|**  (union) 
and **eps** (empty rule) belong to the BNF notation. 
All other symbols are terminals.

  | //Program// | -> | //[Decl]// 
  | //[Decl]// | -> | **eps**
  |  |  **|**  | //Decl// //[Decl]// 
  | //Decl// | -> | ``def`` //LExpr// ``(`` //[Arg]// ``)`` //Guard// ``{`` //[Stmt]// ``}`` 
  |  |  **|**  | //Stmt// 
  | //[Arg]// | -> | **eps**
  |  |  **|**  | //Arg// 
  |  |  **|**  | //Arg// ``,`` //[Arg]// 
  | //[Stmt]// | -> | **eps**
  |  |  **|**  | //Stmt// //[Stmt]// 
  | //Guard// | -> | **eps**
  |  |  **|**  | ``:`` //Type// 
  | //Arg// | -> | //PIdent// //Guard// 
  | //Stmt// | -> | //Expr// //EndLine// 
  |  |  **|**  | ``var`` //LExpr// //Guard// //EndLine// 
  |  |  **|**  | ``var`` ``[`` //LExpr// ``]`` //Guard// //EndLine// 
  |  |  **|**  | ``var`` //LExpr// //Guard// ``:=`` //Expr// //EndLine// 
  |  |  **|**  | ``def`` //LExpr// //Guard// ``:=`` //Expr// //EndLine// 
  |  |  **|**  | ``var`` //LExpr// //Guard// ``:=`` //TypeIter// //EndLine// 
  |  |  **|**  | ``def`` //LExpr// //Guard// ``:=`` //TypeIter// //EndLine// 
  |  |  **|**  | ``return`` //[Expr]// //EndLine// 
  |  |  **|**  | ``{`` //[Decl]// ``}`` 
  |  |  **|**  | ``if`` ``(`` //Expr// ``)`` //Stmt// ``else`` //Stmt// 
  |  |  **|**  | ``if`` ``(`` //Expr// ``)`` //Stmt// 
  |  |  **|**  | ``switch`` ``(`` //Expr// ``)`` ``{`` //[NormCase]// //[DfltCase]// ``}`` 
  |  |  **|**  | ``break`` 
  |  |  **|**  | ``continue`` 
  |  |  **|**  | ``while`` ``(`` //Expr// ``)`` //Stmt// 
  |  |  **|**  | ``for`` //PIdent// ``in`` //TypeIter// //Stmt// 
  | //NormCase// | -> | ``match`` //Expr// //Stmt// 
  | //DfltCase// | -> | ``match _`` //Stmt// 
  | //[NormCase]// | -> | **eps**
  |  |  **|**  | //NormCase// //[NormCase]// 
  | //[DfltCase]// | -> | **eps**
  |  |  **|**  | //DfltCase// //[DfltCase]// 
  | //LExpr// | -> | //PIdent// 
  |  |  **|**  | //Deref// 
  |  |  **|**  | //Ref// 
  | //Deref// | -> | ``&`` //LExpr// 
  | //Ref// | -> | ``*`` //LExpr// 
  | //Expr15// | -> | //LExpr// 
  |  |  **|**  | //Integer// 
  |  |  **|**  | //Double// 
  |  |  **|**  | //Char// 
  |  |  **|**  | //String// 
  |  |  **|**  | ``true`` 
  |  |  **|**  | ``false`` 
  |  |  **|**  | //PIdent// ``(`` //[Arg]// ``)`` 
  |  |  **|**  | ``(`` //Expr// ``)`` 
  | //Expr14// | -> | ``!`` //Expr15// 
  |  |  **|**  | ``-`` //Expr15// 
  |  |  **|**  | //Expr15// 
  | //Expr13// | -> | //Expr14// ``**`` //Expr13// 
  |  |  **|**  | //Expr14// 
  | //Expr12// | -> | //Expr12// ``*`` //Expr13// 
  |  |  **|**  | //Expr12// ``/`` //Expr13// 
  |  |  **|**  | //Expr12// ``//`` //Expr13// 
  |  |  **|**  | //Expr12// ``%`` //Expr13// 
  |  |  **|**  | //Expr12// ``%%`` //Expr13// 
  |  |  **|**  | //Expr13// 
  | //Expr11// | -> | //Expr11// ``+`` //Expr12// 
  |  |  **|**  | //Expr11// ``-`` //Expr12// 
  |  |  **|**  | //Expr12// 
  | //Expr10// | -> | //Expr10// ``..`` //Expr11// 
  |  |  **|**  | //Expr10// ``..!`` //Expr11// 
  |  |  **|**  | //Expr11// 
  | //Expr9// | -> | //Expr9// ``<`` //Expr10// 
  |  |  **|**  | //Expr9// ``>`` //Expr10// 
  |  |  **|**  | //Expr9// ``<=`` //Expr10// 
  |  |  **|**  | //Expr9// ``>=`` //Expr10// 
  |  |  **|**  | //Expr10// 
  | //Expr8// | -> | //Expr8// ``==`` //Expr9// 
  |  |  **|**  | //Expr8// ``!=`` //Expr9// 
  |  |  **|**  | //Expr9// 
  | //Expr4// | -> | //Expr4// ``&&`` //Expr5// 
  |  |  **|**  | //Expr5// 
  | //Expr3// | -> | //Expr3// ``||`` //Expr4// 
  |  |  **|**  | //Expr4// 
  | //Expr2// | -> | //LExpr// //AssignOperator// //Expr2// 
  |  |  **|**  | //Expr3// 
  | //Expr// | -> | //Expr1// 
  | //Expr1// | -> | //Expr2// 
  | //Expr5// | -> | //Expr6// 
  | //Expr6// | -> | //Expr7// 
  | //Expr7// | -> | //Expr8// 
  | //[Expr]// | -> | **eps**
  |  |  **|**  | //Expr// 
  |  |  **|**  | //Expr// ``,`` //[Expr]// 
  | //AssignOperator// | -> | ``:=`` 
  |  |  **|**  | ``|=`` 
  |  |  **|**  | ``&=`` 
  |  |  **|**  | ``+=`` 
  |  |  **|**  | ``-=`` 
  |  |  **|**  | ``*=`` 
  |  |  **|**  | ``//=`` 
  |  |  **|**  | ``/=`` 
  |  |  **|**  | ``%=`` 
  |  |  **|**  | ``%%=`` 
  |  |  **|**  | ``**=`` 
  | //Type// | -> | ``bool`` 
  |  |  **|**  | ``double`` 
  |  |  **|**  | ``int`` 
  |  |  **|**  | ``void`` 
  |  |  **|**  | ``char`` 
  |  |  **|**  | ``string`` 
  |  |  **|**  | //CompoundType// 
  | //CompoundType// | -> | //Type// ``*`` 
  |  |  **|**  | //TypeIter// 
  | //TypeIter// | -> | //Expr10// 
  |  |  **|**  | ``[`` //[Expr]// ``]`` 
  | //EndLine// | -> | ``;`` 


